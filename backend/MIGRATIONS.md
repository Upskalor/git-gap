"""Alembic configuration for database migrations."""
# This is a template for Alembic migrations support
# To fully initialize:
# 1. Install alembic: pip install alembic
# 2. Run: alembic init alembic
# 3. Configure sqlalchemy.url in alembic.ini
# 4. Update alembic/env.py to use async sqlalchemy

"""
Quick Start for Migrations:

1. Initialize alembic:
   alembic init alembic

2. In alembic/env.py, update:
   from app.core.database import Base
   target_metadata = Base.metadata

3. Create migration:
   alembic revision --autogenerate -m "Initial migration"

4. Apply migration:
   alembic upgrade head

5. Downgrade:
   alembic downgrade -1
"""

# Basic migration template will be auto-generated by alembic init
